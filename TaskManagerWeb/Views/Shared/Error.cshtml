@model ErrorViewModel
@{
    ViewData["Title"] = "Errore";
}

<div class="container d-flex flex-column align-items-center justify-content-center text-center py-5" style="min-height:70vh;">
    <div class="mb-4">
        <i class="bi bi-exclamation-triangle-fill text-danger" style="font-size:4rem;"></i>
    </div>

    <h1 class="fw-bold text-danger mb-2">Oops! Qualcosa è andato storto.</h1>
    <p class="text-muted mb-4">
        Si è verificato un errore durante l'elaborazione della tua richiesta.<br />
        Ti invitiamo a riprovare o a contattare l'amministratore se il problema persiste.
    </p>

    @if (Model.ShowRequestId)
    {
        <p class="small text-secondary mb-4">
            <strong>ID richiesta:</strong> <code>@Model.RequestId</code>
        </p>
    }

    <a asp-area="" asp-controller="Home" asp-action="Index"
       class="btn btn-primary rounded-3 px-4 py-2">
        <i class="bi bi-house-door me-1"></i> Torna alla Home
    </a>

    <div class="mt-5 text-start bg-light rounded-3 shadow-sm p-4" style="max-width:600px;">
        <h5 class="fw-semibold text-secondary mb-2">
            <i class="bi bi-tools me-2"></i> Modalità di sviluppo
        </h5>
        <p class="text-muted small mb-0">
            Imposta la variabile <code>ASPNETCORE_ENVIRONMENT</code> su <strong>Development</strong> per visualizzare informazioni dettagliate sugli errori durante il debug locale.<br />
            Non abilitare la modalità <strong>Development</strong> in produzione: potrebbe esporre dati sensibili.
        </p>
    </div>
</div>
